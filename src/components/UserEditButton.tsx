"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { Pencil, X } from "lucide-react";
export function UserEditButton({user,branches,positions}:{user:any;branches:any[];positions:any[]}){const router=useRouter();const[open,setOpen]=useState(false);async function save(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const response=await fetch(`/api/users/${user.id}`,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});if(!response.ok)return alert((await response.json()).error||"Could not update user.");setOpen(false);router.refresh();}return <>{<button className="btn-secondary" onClick={()=>setOpen(true)}><Pencil size={15}/> Edit</button>}{open&&<div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-4"><form onSubmit={save} className="panel grid w-full max-w-3xl gap-3 p-5 md:grid-cols-2"><div className="col-span-full flex justify-between"><h2 className="text-lg font-bold">Update user</h2><button type="button" onClick={()=>setOpen(false)}><X/></button></div><input className="input" name="employeeNo" defaultValue={user.employeeNo} required/><input className="input" name="fullName" defaultValue={user.fullName} required/><input className="input" name="email" type="email" defaultValue={user.email} required/><input className="input" name="username" defaultValue={user.username} required/><select className="input" name="branchId" defaultValue={user.branchId}>{branches.map(b=><option key={b.id} value={b.id}>{b.branchCode} / {b.branchName}</option>)}</select><select className="input" name="positionId" defaultValue={user.positionId||""} required><option value="" disabled>Select position</option>{positions.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select className="input" name="status" defaultValue={user.status}><option>ACTIVE</option><option>INACTIVE</option></select><div className="flex gap-2"><button className="btn-primary">Save changes</button><button type="button" className="btn-secondary" onClick={()=>setOpen(false)}>Cancel</button></div></form></div>}</>}
